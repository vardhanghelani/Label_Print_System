# Point frontend at your Render backend (include /api)
VITE_API_URL=https://label-print-system.onrender.com/api

# Leave unset for local dev — Vite proxies /api → http://localhost:5000

# Set true to log every print text item to browser console before rendering
# VITE_PRINT_DEBUG=true
